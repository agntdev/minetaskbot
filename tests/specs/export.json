[
  {
    "name": "/export shows empty state when no tasks",
    "steps": [
      {
        "send": { "text": "/export" },
        "expect": [
          {
            "method": "sendMessage",
            "payload": { "text": "No tasks to export." }
          }
        ]
      }
    ]
  }
]
