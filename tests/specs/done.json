[
  {
    "name": "/done shows empty state when no tasks",
    "steps": [
      {
        "send": { "text": "/done" },
        "expect": [
          {
            "method": "sendMessage",
            "payload": { "text": "No pending tasks to mark done." }
          }
        ]
      }
    ]
  }
]
