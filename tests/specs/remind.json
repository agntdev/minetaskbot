[
  {
    "name": "/remind shows empty state when no tasks",
    "steps": [
      {
        "send": { "text": "/remind" },
        "expect": [
          {
            "method": "sendMessage",
            "payload": { "text": "No active tasks to set reminders for." }
          }
        ]
      }
    ]
  }
]
