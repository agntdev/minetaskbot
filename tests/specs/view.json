[
  {
    "name": "/view shows empty state when no tasks",
    "steps": [
      {
        "send": { "text": "/view" },
        "expect": [
          {
            "method": "sendMessage",
            "payload": { "text": "No tasks to view — tap Add Task to create one." }
          }
        ]
      }
    ]
  }
]
