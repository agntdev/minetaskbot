[
  {
    "name": "/list shows empty state when no tasks",
    "steps": [
      {
        "send": { "text": "/list" },
        "expect": [
          {
            "method": "sendMessage",
            "payload": { "text": "No tasks yet — tap Add Task to create one." }
          }
        ]
      }
    ]
  }
]
